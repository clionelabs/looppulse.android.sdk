<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.clieonelabs.looppulse.sdk" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <service
            android:name="com.estimote.sdk.service.BeaconService"
            android:exported="false" />

        <receiver
            android:name="com.clionelabs.looppulse.sdk.receivers.RangingAlarmReceiver"
            android:enabled="true"
            android:exported="false" >
            <intent-filter>
                <action android:name="com.clionelabs.looppulse.sdk.action.RANGE">
                </action>
            </intent-filter>
        </receiver>

        <service
            android:name="com.clionelabs.looppulse.sdk.services.RangingService"
            android:enabled="true"
            android:exported="true" >
        </service>
        <service
            android:name="com.clionelabs.looppulse.sdk.services.DataStoreService"
            android:enabled="true"
            android:exported="true" >
        </service>
    </application>

</manifest>
